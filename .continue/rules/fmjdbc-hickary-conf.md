---
description: HickaryCP configuration
---

# HickaryCP FileMaker JDBC configuration

The filemaker JDBC driver is fmjdbc.jar, the driver class is com.filemaker.jdbc.Driver, the hibernate dialect to use is org.hibernate.community.dialect.FileMakerDialect and the connection test sql query to use in that case is "SELECT p.* FROM FileMaker_Tables p"
